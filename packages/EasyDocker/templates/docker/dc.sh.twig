#!/usr/bin/env bash

# Run a composer command inside of the docker containers.

/usr/local/bin/docker-compose -f docker-compose.yml -f docker-compose.local.yml -f docker-compose.dev.yml run --rm composer "$@" || exit 1
