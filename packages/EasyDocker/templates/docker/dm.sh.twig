#!/usr/bin/env bash

# Run the migration container inside the docker containers.

/usr/local/bin/docker-compose -f docker-compose.yml -f docker-compose.local.yml -f docker-compose.dev.yml run --rm migrate || exit 1
