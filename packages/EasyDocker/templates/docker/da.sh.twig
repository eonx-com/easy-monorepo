#!/usr/bin/env bash

# Run an artisan command inside the docker containers.

/usr/local/bin/docker-compose -f docker-compose.yml -f docker-compose.local.yml -f docker-compose.dev.yml run --rm artisan "$@"
