parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            symplify/monorepo-builder: '^4.7'

    # for "split" command
    directories_to_repositories:
        packages/Pagination: 'git@github.com:StepTheFkUp/Pagination.git'
